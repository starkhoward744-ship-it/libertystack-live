const CONFIG={calendlyUrl:"https://calendly.com/ishant-libertystack/new-meeting"};
const qs=(s,c=document)=>c.querySelector(s),qsa=(s,c=document)=>[...c.querySelectorAll(s)];
qsa('[data-year]').forEach(e=>e.textContent=new Date().getFullYear());
const header=qs('#siteHeader');addEventListener('scroll',()=>header?.classList.toggle('scrolled',scrollY>20),{passive:true});
const menuBtn=qs('.menu-toggle'),menu=qs('.mobile-menu');menuBtn?.addEventListener('click',()=>{const o=menu.classList.toggle('open');document.body.classList.toggle('menu-open',o);menuBtn.setAttribute('aria-expanded',o);menu.setAttribute('aria-hidden',!o)});
const observer=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');observer.unobserve(e.target)}}),{threshold:.12});qsa('.reveal').forEach(e=>observer.observe(e));
if(matchMedia('(pointer:fine)').matches){const dot=document.createElement('div'),ring=document.createElement('div');dot.className='cursor-dot';ring.className='cursor-ring';document.body.append(dot,ring);addEventListener('mousemove',e=>{dot.style.transform=`translate(${e.clientX-2}px,${e.clientY-2}px)`;ring.animate({transform:`translate(${e.clientX-16}px,${e.clientY-16}px)`},{duration:220,fill:'forwards'})});qsa('a,button,input,select,textarea,.tilt').forEach(e=>{e.addEventListener('mouseenter',()=>ring.classList.add('hover'));e.addEventListener('mouseleave',()=>ring.classList.remove('hover'))})}
qsa('.tilt').forEach(card=>{card.addEventListener('mousemove',e=>{const r=card.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;card.style.transform=`perspective(900px) rotateX(${-y*6}deg) rotateY(${x*8}deg) translateY(-3px)`});card.addEventListener('mouseleave',()=>card.style.transform='')});
const ba=qs('[data-before-after]');if(ba){const before=qs('.before-panel',ba),handle=qs('.ba-handle',ba);const move=x=>{const r=ba.getBoundingClientRect(),p=Math.max(5,Math.min(95,(x-r.left)/r.width*100));before.style.width=p+'%';handle.style.left=p+'%'};ba.addEventListener('pointermove',e=>{if(e.buttons||e.pointerType==='touch')move(e.clientX)});ba.addEventListener('click',e=>move(e.clientX))}
function money(n){return new Intl.NumberFormat('en-US',{style:'currency',currency:'USD',maximumFractionDigits:0}).format(n)}
const visitors=qs('#visitors'),cv=qs('#customerValue'),conv=qs('#conversion');function calcROI(){if(!visitors)return;qs('#visitorsOut').textContent=Number(visitors.value).toLocaleString('en-US');qs('#valueOut').textContent=money(cv.value);qs('#conversionOut').textContent=Number(conv.value).toFixed(1)+'%';qs('#roiResult').textContent=money(Number(visitors.value)*.006*Number(cv.value))}[visitors,cv,conv].forEach(e=>e?.addEventListener('input',calcROI));calcROI();
const quoteChecks=qsa('.quote-options input');function updateQuote(){if(!quoteChecks.length)return;const total=quoteChecks.filter(x=>x.checked).reduce((s,x)=>s+Number(x.value),0);qs('#quoteTotal').textContent=`${money(total)} – ${money(Math.round(total*1.3))}`}quoteChecks.forEach(x=>x.addEventListener('change',updateQuote));updateQuote();qs('#useQuote')?.addEventListener('click',()=>{const chosen=quoteChecks.filter(x=>x.checked).map(x=>x.dataset.label).join(', ');const ta=qs('[name=message]');if(ta){ta.value=`I am interested in: ${chosen}. Indicative range shown: ${qs('#quoteTotal').textContent}.`;ta.scrollIntoView({behavior:'smooth',block:'center'})}});

qsa('.calendly-link').forEach(a=>{if(CONFIG.calendlyUrl){a.href=CONFIG.calendlyUrl;a.target='_blank';a.rel='noopener'}});
const launcher=qs('.chat-launcher'),panel=qs('.chat-panel'),close=qs('.chat-close'),messages=qs('.chat-messages');function openChat(o=true){panel?.classList.toggle('open',o);panel?.setAttribute('aria-hidden',!o)}launcher?.addEventListener('click',()=>openChat(true));close?.addEventListener('click',()=>openChat(false));function addMsg(t,who='bot'){const d=document.createElement('div');d.className=who==='user'?'user-message':'bot-message';d.textContent=t;messages.appendChild(d);messages.scrollTop=messages.scrollHeight}const responses={services:'For most businesses, I recommend starting with a conversion-focused website. Add CRM automation when slow follow-up or manual lead handling is the constraint.',budget:'Focused website projects start around $499. Website + automation engagements commonly start around $999. The instant quote generator on the contact page gives a rough range.',call:'Use “Book a strategy call” to open LibertyStack’s Calendly page and choose a convenient time.'};qsa('[data-chat]').forEach(b=>b.addEventListener('click',()=>{addMsg(b.textContent,'user');setTimeout(()=>addMsg(responses[b.dataset.chat]),250)}));qs('.chat-form')?.addEventListener('submit',e=>{e.preventDefault();const inp=qs('input',e.currentTarget),t=inp.value.trim();if(!t)return;addMsg(t,'user');inp.value='';setTimeout(()=>addMsg('Thanks — based on that, the best next step is a short discovery call or project brief. You can use the contact page, and the details will be sent to hello@libertystack.co.in.'),350)});
const canvas=qs('#networkGlobe');if(canvas&&!matchMedia('(prefers-reduced-motion:reduce)').matches){const ctx=canvas.getContext('2d');let pts=[],w,h,dpr=Math.min(devicePixelRatio,2),mx=0,my=0;function resize(){w=canvas.clientWidth;h=canvas.clientHeight;canvas.width=w*dpr;canvas.height=h*dpr;ctx.setTransform(dpr,0,0,dpr,0,0);pts=Array.from({length:75},(_,i)=>{const phi=Math.acos(-1+2*i/75),theta=Math.sqrt(75*Math.PI)*phi;return{x:Math.cos(theta)*Math.sin(phi),y:Math.sin(theta)*Math.sin(phi),z:Math.cos(phi)}})}new ResizeObserver(resize).observe(canvas);canvas.addEventListener('mousemove',e=>{const r=canvas.getBoundingClientRect();mx=(e.clientX-r.left)/r.width-.5;my=(e.clientY-r.top)/r.height-.5});let t=0;function frame(){ctx.clearRect(0,0,w,h);t+=.002;const s=Math.min(w,h)*.34,cx=w*.58,cy=h*.5,project=[];for(const p of pts){let x=p.x*Math.cos(t)-p.z*Math.sin(t),z=p.x*Math.sin(t)+p.z*Math.cos(t),y=p.y;const ay=mx*.35,ax=my*.25,xx=x*Math.cos(ay)-z*Math.sin(ay),zz=x*Math.sin(ay)+z*Math.cos(ay),yy=y*Math.cos(ax)-zz*Math.sin(ax),zzz=y*Math.sin(ax)+zz*Math.cos(ax);project.push({x:cx+xx*s,y:cy+yy*s,z:zzz})}ctx.lineWidth=.5;for(let i=0;i<project.length;i++)for(let j=i+1;j<project.length;j++){const a=project[i],b=project[j],d=Math.hypot(a.x-b.x,a.y-b.y);if(d<75&&a.z>-.5&&b.z>-.5){ctx.strokeStyle=`rgba(130,150,255,${(1-d/75)*.25})`;ctx.beginPath();ctx.moveTo(a.x,a.y);ctx.lineTo(b.x,b.y);ctx.stroke()}}for(const p of project){if(p.z<-.5)continue;ctx.fillStyle=`rgba(160,205,255,${.22+(p.z+1)*.22})`;ctx.beginPath();ctx.arc(p.x,p.y,1.2+(p.z+1)*.6,0,7);ctx.fill()}requestAnimationFrame(frame)}frame()}
qsa('a[href]').forEach(a=>{const u=a.getAttribute('href');if(!u||!u.includes('.html'))return;a.addEventListener('click',e=>{if(e.ctrlKey||e.metaKey||e.shiftKey||a.target==='_blank')return;e.preventDefault();document.body.classList.add('page-leaving');setTimeout(()=>location.href=u,420)})});
addEventListener('pageshow',()=>document.body.classList.remove('page-leaving'));
qsa('.social-icon').forEach(icon=>icon.addEventListener('click',()=>{icon.classList.remove('social-clicked');void icon.offsetWidth;icon.classList.add('social-clicked');setTimeout(()=>icon.classList.remove('social-clicked'),700)}));